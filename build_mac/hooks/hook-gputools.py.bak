"""
pyinstaller hook that collects collects the missing
data files from gputools (mostly opencl kernels)
"""

import sys
import os
sys.path.append(os.path.split(os.path.abspath(__file__))[0])
from hook_utils import _get_toc_objects


import gputools

__PATH__ = os.path.dirname(gputools.__file__)

# excludedimports = ["PyQt5", "PyQt4"]

datas = _get_toc_objects(os.path.join(__PATH__, ""),
                         dir_prefix = "",
                         flatten_dir = True,
                         filter_str = ".*\.cl")


print "\n"*5
print datas
print "\n"*5
